var arrayCard = [];
var creditCard = parseInt(prompt('Insira aqui o número do seu cartão de crédito.'));
  for(var i = 0; i < creditCard.length; i++){
    arrayCard.push(creditCard[i]);
  isValidCard(creditCard);
  }

  var arrayInvert = arrayCard.reverse();


function isValidCard(cardNumber){
}
